组件挂载 更新 销毁触发的一系列函数
before created
mounted    请求数据 操作dom
updated
destroyed


父子传值
1.动态绑定 
2.子组件中 props 接受父传来的值

子传父
1.调用子组件时候定义一个ref
2.父组件用 this.$refs.header.方法

子主动获取父的
this.$parent.

非父子
1.利用父子
2.bus
        新建一个js文件 引入vue 实例化 暴露实例
        引用这个实例 方法  $emit（名称 数据）
        接受监听mounted 实例.

适配器  rem 
get post get的url会被隐藏 长度限制 参数会被浏览器保留 不安全
计算属性 计算属性绑定的值以来的任何值改变会更新dom
watch 监听变量 变化 调用方法
vfor中key可以判断是否变化  变化的话更新渲染
水平居中 ：行级元素：text-algin  margin auto
            display：flex
            margin left  长度固定
            tansform
position :absolute fixed relative
foreach map     var a = [1,2,3,4,5]
                var b = a.map((item) => {
                    return item = item * 2
                })
                console.log(a)  // [1,2,3,4,5]
                console.log(b)  // [2,4,6,8,10] 

                var a = [1,2,3,4,5]
                a.forEach((item) => {
                    item = item * 2
                })
                console.log(a)